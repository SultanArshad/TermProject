<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fasssoft.eventhallfinder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.LoginSinup.signin" />
        <activity android:name=".views.LoginSinup.signup" />
        <activity android:name=".views.Wellcome" />
        <activity android:name=".views.EditHallDetail" />
        <activity android:name=".views.FragmentActivities.ShowHallsListActivity" />
        <activity android:name=".views.FragmentActivities.ShowHallsDetailActivity" />
    </application>

</manifest>